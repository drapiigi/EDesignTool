package com.ghana.gwire.domain.floorplan;

/**
 * Drawing layers for plan visibility (Phase 13a).
 */
public enum LayerId {
    /** Rooms, walls, doors, windows, background. */
    ARCHITECTURE,
    /** Devices, wiring routes, electrical annotations. */
    ELECTRICAL
}
